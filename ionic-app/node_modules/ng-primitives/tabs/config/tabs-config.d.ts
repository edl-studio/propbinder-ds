import { InjectionToken, Provider } from '@angular/core';
import { NgpOrientation } from 'ng-primitives/common';
export interface NgpTabsConfig {
    /**
     * The orientation of the tabset
     * @default 'horizontal'
     */
    orientation: NgpOrientation;
    /**
     * Whether tabs should activate on focus
     * @default true
     */
    activateOnFocus: boolean;
    /**
     * Whether focus should wrap within the tab list when using the keyboard.
     * @default true
     */
    wrap: boolean;
}
export declare const defaultTabsConfig: NgpTabsConfig;
export declare const NgpTabsConfigToken: InjectionToken<NgpTabsConfig>;
/**
 * Provide the default Tabs configuration
 * @param config The Tabs configuration
 * @returns The provider
 */
export declare function provideTabsConfig(config: Partial<NgpTabsConfig>): Provider[];
/**
 * Inject the Tabs configuration
 * @returns The global Tabs configuration
 */
export declare function injectTabsConfig(): NgpTabsConfig;
