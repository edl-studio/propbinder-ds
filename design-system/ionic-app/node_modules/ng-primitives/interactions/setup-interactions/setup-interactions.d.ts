import { Signal } from '@angular/core';
export interface NgpInteractionOptions {
    hover?: boolean;
    press?: boolean;
    focus?: boolean;
    focusWithin?: boolean;
    focusVisible?: boolean;
    disabled?: Signal<boolean>;
}
/**
 * Setup the interactions without relying on HostDirectives.
 */
export declare function setupInteractions({ focus, hover, press, focusWithin, focusVisible, disabled, }: NgpInteractionOptions): void;
