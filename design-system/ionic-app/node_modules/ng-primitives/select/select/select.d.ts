import { BooleanInput } from '@angular/cdk/coercion';
import { Injector } from '@angular/core';
import type { Placement } from '@floating-ui/dom';
import type { NgpSelectDropdown } from '../select-dropdown/select-dropdown';
import { NgpSelectOption } from '../select-option/select-option';
import type { NgpSelectPortal } from '../select-portal/select-portal';
import * as i0 from "@angular/core";
/**
 * Ideally we would use a generic type here, unfortunately, unlike in React,
 * we cannot infer the type based on another input. For example, if multiple
 * is true, we cannot infer that the value is an array of T. Using a union
 * type is not ideal either because it requires the user to handle multiple cases.
 * Using a generic also isn't ideal because we need to use T as both an array and
 * a single value.
 *
 * Any seems to be used by Angular Material, ng-select, and other libraries
 * so we will use it here as well.
 */
type T = any;
export declare class NgpSelect {
    /** Access the select configuration. */
    protected readonly config: import("../config/select-config").NgpSelectConfig;
    /** @internal Access the select element. */
    readonly elementRef: import("@angular/core").ElementRef<HTMLElement>;
    /** Access the injector. */
    protected readonly injector: Injector;
    /** The unique id of the select. */
    readonly id: import("@angular/core").InputSignal<string>;
    /** The value of the select. */
    readonly value: import("@angular/core").InputSignal<any>;
    /** Event emitted when the value changes. */
    readonly valueChange: import("@angular/core").OutputEmitterRef<any>;
    /** Whether the select is multiple selection. */
    readonly multiple: import("@angular/core").InputSignalWithTransform<boolean, BooleanInput>;
    /** Whether the select is disabled. */
    readonly disabled: import("@angular/core").InputSignalWithTransform<boolean, BooleanInput>;
    /** Emit when the dropdown open state changes. */
    readonly openChange: import("@angular/core").OutputEmitterRef<boolean>;
    /** The comparator function used to compare options. */
    readonly compareWith: import("@angular/core").InputSignal<(a: T | undefined, b: T | undefined) => boolean>;
    /** The position of the dropdown. */
    readonly placement: import("@angular/core").InputSignal<Placement>;
    /** The container for the dropdown. */
    readonly container: import("@angular/core").InputSignal<string | HTMLElement | null>;
    /**
     * Store the select portal.
     * @internal
     */
    readonly portal: import("@angular/core").WritableSignal<NgpSelectPortal | undefined>;
    /**
     * Store the select dropdown.
     * @internal
     */
    readonly dropdown: import("@angular/core").WritableSignal<NgpSelectDropdown | undefined>;
    /**
     * Store the select options.
     * @internal
     */
    readonly options: import("@angular/core").WritableSignal<NgpSelectOption[]>;
    /**
     * Access the overlay
     * @internal
     */
    readonly overlay: import("@angular/core").Signal<import("ng-primitives/portal").NgpOverlay<void> | null | undefined>;
    /**
     * The open state of the select.
     * @internal
     */
    readonly open: import("@angular/core").Signal<boolean>;
    /**
     * The active key descendant manager.
     * @internal
     */
    readonly activeDescendantManager: {
        activeDescendant: import("@angular/core").Signal<string | undefined>;
        activeItem: import("@angular/core").Signal<NgpSelectOption | undefined>;
        activate: (item: NgpSelectOption | undefined) => void;
        first: () => void;
        last: () => void;
        next: () => void;
        previous: () => void;
        reset: () => void;
    };
    /** The state of the select. */
    protected readonly state: import("ng-primitives/state").CreatedState<NgpSelect>;
    constructor();
    /**
     * Open the dropdown.
     * @internal
     */
    openDropdown(): Promise<void>;
    /**
     * Close the dropdown.
     * @internal
     */
    closeDropdown(): void;
    /**
     * Toggle the dropdown.
     * @internal
     */
    toggleDropdown(): Promise<void>;
    /**
     * Select an option.
     * @param option The option to select.
     * @internal
     */
    selectOption(option: NgpSelectOption | undefined): void;
    /**
     * Deselect an option.
     * @param option The option to deselect.
     * @internal
     */
    deselectOption(option: NgpSelectOption): void;
    /**
     * Toggle the selection of an option.
     * @param option The option to toggle.
     * @internal
     */
    toggleOption(option: NgpSelectOption): void;
    /**
     * Determine if an option is selected.
     * @param option The option to check.
     * @internal
     */
    isOptionSelected(option: NgpSelectOption): boolean;
    /**
     * Activate the next option in the list if there is one.
     * If there is no option currently active, activate the selected option or the first option.
     * @internal
     */
    activateNextOption(): void;
    /**
     * Activate the previous option in the list if there is one.
     * @internal
     */
    activatePreviousOption(): void;
    /**
     * Register the dropdown portal with the select.
     * @param portal The dropdown portal.
     * @internal
     */
    registerPortal(portal: NgpSelectPortal): void;
    /**
     * Register the dropdown with the select.
     * @param dropdown The dropdown to register.
     * @internal
     */
    registerDropdown(dropdown: NgpSelectDropdown): void;
    /**
     * Register an option with the select.
     * @param option The option to register.
     * @internal
     */
    registerOption(option: NgpSelectOption): void;
    /**
     * Unregister an option from the select.
     * @param option The option to unregister.
     * @internal
     */
    unregisterOption(option: NgpSelectOption): void;
    /**
     * Focus the select.
     * @internal
     */
    focus(): void;
    /** Handle keydown events for accessibility. */
    protected handleKeydown(event: KeyboardEvent): void;
    protected onBlur(event: FocusEvent): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<NgpSelect, never>;
    static ɵdir: i0.ɵɵDirectiveDeclaration<NgpSelect, "[ngpSelect]", ["ngpSelect"], { "id": { "alias": "id"; "required": false; "isSignal": true; }; "value": { "alias": "ngpSelectValue"; "required": false; "isSignal": true; }; "multiple": { "alias": "ngpSelectMultiple"; "required": false; "isSignal": true; }; "disabled": { "alias": "ngpSelectDisabled"; "required": false; "isSignal": true; }; "compareWith": { "alias": "ngpSelectCompareWith"; "required": false; "isSignal": true; }; "placement": { "alias": "ngpSelectDropdownPlacement"; "required": false; "isSignal": true; }; "container": { "alias": "ngpSelectDropdownContainer"; "required": false; "isSignal": true; }; }, { "valueChange": "ngpSelectValueChange"; "openChange": "ngpSelectOpenChange"; }, never, never, true, never>;
}
export {};
